---
title: "match statement"
sidebarTitle: "match statement"
# 
tags:
  - python
lastUpdated: 2026-04-08T09:00:42.959Z
---

```python title= showLineNumbers
def http_error(status):
  match status:
      case 400:
          return "Bad request"
      case 404:
          return "Not found"
      case "this" | "that":
          return "Match either this or that"
      case (0, 0):
        print("Match a tuple 0,0")
      case _:
          return "Something's wrong with the internet"
```

Only the first pattern that matches gets executed

<Note> 

the variable `_` acts as a wildcard and never fails to match

</Note>

<span style={{color: "var(--can-not-understand-font-color)"}}> it can also extract components (sequence elements or object attributes) from the value into variables. </span>
<br/>

---

# Sources

- https://docs.python.org/3/tutorial/controlflow.html#match-statements
- https://peps.python.org/pep-0636/
