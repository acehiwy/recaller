---
title: "Set"
sidebarTitle: "Set"
lastUpdated: 2026-04-08T09:00:42.959Z
tags:
  - python
---

import { Callout } from '/snippets/callout.mdx';

A set is an <span style={{color: "var(--secondary-font-color)"}}> unordered </span> collection with no duplicate elements.

``` python lines
basket = {'apple', 'orange', 'apple', 'pear', 'orange', 'banana'}

empty_set = set() 
```

## union, intersection, difference, and symmetric difference

``` python lines
a = set('abcdr')
b = set('acmlz')

a - b # left join. letters in a but not in b
# {'r', 'd', 'b'}

a | b # full join. letters in a or b or both
# {'a', 'c', 'r', 'd', 'b', 'm', 'z', 'l'}

a & b # inner join. letters in both a and b
# {'a', 'c'}

a ^ b # outer join. letters in a or b but not both
# {'r', 'd', 'b', 'm', 'z', 'l'}
```

## Set comprehensions

``` python lines
a = {x for x in 'abracadabra' if x not in 'abc'}
# {'r', 'd'}
```

## Miscellaneous

<Callout type="info"> 

More info on set [here](https://docs.python.org/3/library/stdtypes.html#set-types-set-frozenset)

</Callout>

<br/>

---

# Sources

- https://docs.python.org/3/tutorial/datastructures.html#sets