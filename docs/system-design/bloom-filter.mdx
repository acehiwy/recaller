---
last_update:
  date: 2026-06-10T12:52:07.479Z
---

If the Bloom filter returns "probably in set," one usually double-checks via a slower method (e.g. doing the full lookup in a database or cache) if exact confirmation is needed.