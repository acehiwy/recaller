---
last_update:
  date: 2026-06-10T12:52:07.479Z
---

How do
you ensure that the data remains correct and complete, even when things go wrong
internally? How do you provide consistently good performance to clients, even when
parts of your system are degraded? How do you scale to handle an increase in load?
What does a good API for the service look like?


## Availability

the system remains operational even in the face of failures or increased demand. 

often mean term of uptime. total operational time

often achieve by
- redunancy (duplicate component, so if one goes down there are other)
- load balancning


## reliability
The application performs the function that the user expected.
• It can tolerate the user making mistakes or using the software in unexpected
ways.
• Its performance is good enough for the required use case, under the expected
load and data volume.
• The system prevents any unauthorized access and abuse.

continuing to work correctly, event when things go wrong



6 The former term is slightly
misleading: it suggests that we could make 

fault is a component not bahave as its spec
failure is when the system stop respond to the user


## hardware fault

hardware could be thee cause of failure. a hardware can live about 10 - 50 years

### typical solution
redunancy component is usually an approach
hard disk -> store duplicate data in multiple hard disk (RAID configuration)
server -> dual power supply
data center -> use batteries or gas power generator


## software error

eg
- a bug in software that crash multiple server instance
- some process that consume lot of CPU or memory

<Admonition type="info" title="info"> 

When software error occur, it reveal that a software had made assumption about its environment.

mostly the assumption was true , but eventually the error would occur

</Admonition>

there is no quick solution for software error
