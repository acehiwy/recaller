name: recaller

services:
  app:
    build:
      dockerfile: local.Dockerfile
    volumes:
      - .:/workspaces
    ports:
      - 22222:22222
    command: bash -c "pnpm i && pnpm start --host 0.0.0.0"
