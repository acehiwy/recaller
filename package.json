{
  "name": "recaller",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "mint dev --no-open",
    "start": "mint dev --no-open",
    "build": "mint validate",
    "export": "mint export --output export.zip",
    "update-last-updated": "node update-last-updated.mjs",
    "wt": "./scripts/setup-worktree",
    "prepare": "husky"
  },
  "devDependencies": {
    "glob": "^11.0.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.3",
    "mint": "4.2.500"
  },
  "engines": {
    "node": ">=20.0"
  },
  "packageManager": "yarn@4.5.3"
}
